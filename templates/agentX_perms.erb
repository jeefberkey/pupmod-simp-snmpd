<%
lstr = "agentXPerms #{@sockperms}"

if !@dirperms.nil? then
    lstr += " #{@dirperms}"
end

if !@user.nil? then
    lstr += " #{@user}"

    if !@group.nil? then
        lstr += " #{@group}"
    end
end
-%>
<%= lstr %>
