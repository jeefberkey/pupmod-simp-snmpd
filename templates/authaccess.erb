<%
lstr = "authaccess #{@types}"

if !@model.nil?
    lstr = "#{lstr} #{@model}"
end

lstr = "#{lstr} #{@group} #{@view}"

if !@level.nil?
    lstr = "#{lstr} #{@level}"

    if !@context.nil?
        lstr = "#{lstr} #{@context}"
    end
end
-%>
<%= lstr %>
